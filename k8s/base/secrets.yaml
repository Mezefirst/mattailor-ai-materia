# Secret template - Replace with actual base64 encoded values
apiVersion: v1
kind: Secret
metadata:
  name: mattailor-secrets
  namespace: mattailor-ai
type: Opaque
data:
  # Base64 encoded values - replace these with actual values
  # echo -n "your-database-url" | base64
  database-url: ""
  # echo -n "your-matproject-api-key" | base64
  matproject-api-key: ""
  # echo -n "your-matweb-api-key" | base64
  matweb-api-key: ""
  # echo -n "your-openai-api-key" | base64
  openai-api-key: ""
  # echo -n "your-postgres-password" | base64
  postgres-password: ""